// Deps
import HeroSection from "~/components/HeroSection";
import PageTransition from "~/components/PageTransition";

const Home = () => {
  return (
    <PageTransition>
      <HeroSection />
    </PageTransition>
  );
};

export default Home;
